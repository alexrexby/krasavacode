# Windows shell scripts must keep CRLF endings even when committed from macOS/Linux,
# otherwise cmd.exe may fail to parse them.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# POSIX shell scripts always LF.
*.command text eol=lf
*.sh text eol=lf
